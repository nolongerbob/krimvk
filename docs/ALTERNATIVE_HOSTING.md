# Альтернативные российские хостинги (бюджетные варианты)

## 💰 Сравнение стоимости

### Yandex Cloud (дорого)
- Managed PostgreSQL: ~13000₽/месяц (минимальный)
- Object Storage: ~500₽/месяц
- VPS/Cloud Run: ~2000₽/месяц
- **Итого: ~15500₽/месяц** ❌

## ✅ Бюджетные альтернативы

### Вариант 1: Timeweb (Рекомендуется для бюджета) ⭐

**Преимущества:**
- ✅ Дешевле в 3-4 раза
- ✅ Простой интерфейс
- ✅ Хорошая поддержка
- ✅ Российская инфраструктура

**Стоимость:**
- VPS (4GB RAM, 2 CPU): ~800₽/месяц
- PostgreSQL на VPS (включен): **0₽** (устанавливаете сами)
- Object Storage: ~300₽/месяц (или храните на VPS)
- **Итого: ~1100-1500₽/месяц** ✅

**Недостатки:**
- Нет managed PostgreSQL (нужно настраивать самим)
- Нужно делать бэкапы вручную

**Подходит если:**
- Нужен бюджетный вариант
- Готовы настраивать PostgreSQL сами
- Не нужен managed сервис

### Вариант 2: Selectel

**Преимущества:**
- ✅ Дешевле Yandex Cloud
- ✅ Есть managed PostgreSQL (дешевле)
- ✅ Хорошая инфраструктура

**Стоимость:**
- Managed PostgreSQL: ~3000-4000₽/месяц
- VPS: ~1000₽/месяц
- Object Storage: ~400₽/месяц
- **Итого: ~4400-5400₽/месяц**

**Подходит если:**
- Нужен managed PostgreSQL, но дешевле Yandex
- Хороший баланс цена/качество

### Вариант 3: REG.RU

**Преимущества:**
- ✅ Очень дешево
- ✅ Простой интерфейс
- ✅ Хорошая поддержка

**Стоимость:**
- VPS (4GB RAM): ~600₽/месяц
- PostgreSQL на VPS: **0₽** (сами)
- Хранилище: включено в VPS
- **Итого: ~600-800₽/месяц** ✅

**Недостатки:**
- Нет managed сервисов
- Меньше функций

### Вариант 4: VPS + PostgreSQL на том же сервере (Самый дешевый)

**Архитектура:**
- Один VPS сервер
- PostgreSQL на том же сервере
- Файлы на том же сервере
- Nginx как reverse proxy

**Стоимость:**
- VPS (4GB RAM, 2 CPU, 50GB SSD): ~800-1000₽/месяц
- **Итого: ~800-1000₽/месяц** ✅✅

**Преимущества:**
- ✅ Самое дешевое решение
- ✅ Все на одном сервере
- ✅ Простая настройка

**Недостатки:**
- Нет автоматических бэкапов (нужно настраивать)
- Нет репликации
- Меньше отказоустойчивости

## 🎯 Рекомендации по выбору

### Если бюджет ограничен (< 2000₽/месяц):
**→ Timeweb или REG.RU VPS**
- Установите PostgreSQL сами
- Настройте автоматические бэкапы
- Все на одном сервере

### Если нужен managed PostgreSQL (< 5000₽/месяц):
**→ Selectel**
- Managed PostgreSQL дешевле Yandex
- Хорошая инфраструктура
- Автоматические бэкапы

### Если нужен максимальный комфорт:
**→ Yandex Cloud** (но дорого)
- Все managed сервисы
- Автоматические бэкапы
- Высокая отказоустойчивость

## 📋 Детальное сравнение

| Хостинг | VPS | Managed PG | Object Storage | Итого/мес |
|---------|-----|------------|----------------|-----------|
| **Yandex Cloud** | 2000₽ | 13000₽ | 500₽ | **15500₽** ❌ |
| **Selectel** | 1000₽ | 3000₽ | 400₽ | **4400₽** |
| **Timeweb** | 800₽ | 0₽* | 300₽ | **1100₽** ✅ |
| **REG.RU** | 600₽ | 0₽* | 0₽* | **600₽** ✅✅ |
| **VPS общий** | 800₽ | 0₽* | 0₽* | **800₽** ✅✅ |

*PostgreSQL устанавливается на VPS самим (бесплатно)

## 💡 Рекомендация для вашего случая

**Выбирайте Timeweb или REG.RU VPS:**

1. **VPS 4GB RAM, 2 CPU** (~800₽/месяц)
2. **Установите PostgreSQL** на том же сервере (бесплатно)
3. **Настройте автоматические бэкапы** (скрипт)
4. **Храните файлы** на том же сервере или отдельном Object Storage

**Итого: ~800-1500₽/месяц вместо 15500₽!**

## 🔧 Что нужно будет сделать

### 1. Установка PostgreSQL на VPS:

```bash
# На Ubuntu/Debian
sudo apt update
sudo apt install postgresql postgresql-contrib

# Создать базу данных
sudo -u postgres createdb krimvk
sudo -u postgres createuser krimvk_user
```

### 2. Настройка бэкапов:

```bash
# Автоматический бэкап каждый день
0 2 * * * pg_dump -U krimvk_user krimvk > /backups/krimvk_$(date +\%Y\%m\%d).sql
```

### 3. Настройка мониторинга:

- Настроить алерты на диск/память
- Мониторинг PostgreSQL
- Логирование ошибок

## 📝 План миграции на бюджетный хостинг

См. `docs/TIMEWEB_SETUP.md` или `docs/REG_RU_SETUP.md` для детальных инструкций.

