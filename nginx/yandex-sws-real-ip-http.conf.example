# Use this ONLY if no other conf.d file sets real_ip_header (no cloudflare-real-ip.conf).
# Copy to /etc/nginx/conf.d/yandex-sws-real-ip.conf

set_real_ip_from 51.250.116.133/32;
real_ip_header X-Forwarded-For;
real_ip_recursive on;
