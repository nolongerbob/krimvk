{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && node scripts/migrate-water-quality.js && next build",
  "installCommand": "npm install",
  "functions": {
    "app/api/admin/water-quality/documents/upload/route.ts": {
      "maxDuration": 300,
      "memory": 2048
    }
  }
}
