{
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "functions": {
    "app/api/admin/water-quality/documents/upload/route.ts": {
      "maxDuration": 300,
      "memory": 2048
    }
  }
}
