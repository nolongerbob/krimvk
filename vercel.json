{
  "buildCommand": "prisma generate && node scripts/migrate-water-quality.js && prisma db push --accept-data-loss && next build",
  "installCommand": "npm install",
  "functions": {
    "app/api/admin/water-quality/documents/upload/route.ts": {
      "maxDuration": 300,
      "memory": 2048
    }
  }
}
